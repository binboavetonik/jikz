/**
 * Built-in shape sets.
 *
 * `basicShapes` is the four primitives; `complexShapes` the rest of the
 * catalogue; `allShapes` both. Import the narrowest set you need —
 * anything you don't import is not in your bundle.
 */
export { basicShapes, defaultShape } from './basic'
export { complexShapes } from './complex'
export { allShapes } from './all'
